defaults:
  -
    scope:
      type: "pages"
    values:
      layout: "default"
  -
    scope:
      type: "posts"
    values:
      layout: "post"

description: "I'm a developer, remote worker, and a hockey fan living in Louisiana. I'm helping Clearbit build APIs and integrations for business data."

exclude:
  - .gitignore
  - Gemfile
  - Gemfile.lock
  - Procfile.dev
  - config.ru
  - css/_*.css
  - node_modules
  - package.json
  - postcss.config.js
  - purgecss.config.js
  - tailwind.config.js
  - yarn.lock

permalink: "/journal/:title/"

plugins:
  - jekyll-postcss
  - jekyll-redirect-from

redirect_from:
  json: false

url: "http://localhost:4000"
